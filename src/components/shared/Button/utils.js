import { ICON_POSITIONS } from './constants';

export const isIconAtStart = iconPosition => iconPosition === ICON_POSITIONS.START;

export const isIconAtEnd = iconPosition => iconPosition === ICON_POSITIONS.END;
